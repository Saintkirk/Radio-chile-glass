# DEPRECATED: do not use npm ci here.
# This repo is packageManager: pnpm@9.12.0.
# Native APK builds live in android-native.yml (Gradle + pnpm).
# EAS APK builds live in android-apk.yml.
#
# Kept as a no-op so old branch rules / badges do not break silently.
name: Build Android APK (Native Gradle) [deprecated]

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Redirect to android-native.yml
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: |
          echo "This workflow is deprecated."
          echo "Use: Actions → Android APK (Native Gradle) → android-native.yml"
          echo "Or: Actions → Android APK → android-apk.yml (EAS)"
          exit 0
